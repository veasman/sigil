name: release

on:
  push:
    tags:
      - 'v*'

jobs:
  tarball:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create source tarball
        run: |
          git archive --format=tar.gz --prefix=sigil-${GITHUB_REF_NAME#v}/ HEAD > sigil-${GITHUB_REF_NAME#v}.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sigil-source
          path: sigil-*.tar.gz
